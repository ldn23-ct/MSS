# E2：缺陷深度–slit 通道匹配

## 状态

待实现；当前仅预留 `scripts/postprocessing/e2/` 和 `results/articlev2/postprocessing/E2/`。旧 schema 分支不是正式实现。

## 预定接口

- 输入：审计通过的 P0、P1–P6 center `events_valid.csv`、metadata 和 condition inventory。
- 通道：只使用现有 `slit_label`，不得重新分配。
- 统计：total/k1/MS 的 P1–P6 × S1–S6 响应及相对 P0 变化。
- 输出：`figures/`、`tables/`、manifest、report、acceptance summary。

实现前需补充零基线策略、误差传播和完整验收合同。
