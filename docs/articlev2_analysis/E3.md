# E3：缺陷诱导 detected source-response redistribution

## 状态

待实现；当前仅预留 `scripts/postprocessing/e3/` 和 `results/articlev2/postprocessing/E3/`。旧 `events_clean/slit_id` 代码仅保存在 `_archive/`。

## 预定接口

- 输入：审计通过的 P0/P1–P6 center valid events，以及对应匹配 slit。
- 分类：total/k1/MS；first-scatter source region 使用几何预定义的 front/target/behind 左闭右开区间。
- 输出：匹配通道计数、first-scatter depth 分布、source-region 分解、figures/tables、manifest、report 与 acceptance summary。

实现不得声称严格分离裸源项变化与后续输运变化。
